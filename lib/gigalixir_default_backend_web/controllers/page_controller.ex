defmodule GigalixirDefaultBackendWeb.PageController do
  use GigalixirDefaultBackendWeb, :controller

  alias GigalixirDefaultBackend.{Repo}

  def index(conn, _params) do
    # with {:ok, app_name} <- parse_app_name(conn.host),
    #      {:ok, app_state} <- current_app_state(app_name),
    #      {:ok, status_code} <- status_code_for(app_state),
    #      {:ok, message} <- message_for(app_state) do
    # else
    #   {:error, 
    # end
    # Repo.get_by(AppDomain
    # render conn, "index.html"
  end
end
